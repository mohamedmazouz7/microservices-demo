apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-pvc
  namespace: shop-zone
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard 
